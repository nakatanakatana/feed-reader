/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../kubb-client.ts";
import type {
  FeedsListQueryResponse,
  FeedsListQueryParams,
  FeedsList500,
} from "../../types-generated.ts";
import type {
  QueryKey,
  QueryClient,
  UseBaseQueryOptions,
  UseQueryResult,
} from "@tanstack/solid-query";
import { feedsList } from "../client/feedsList.ts";
import { queryOptions, useQuery } from "@tanstack/solid-query";

export const feedsListQueryKey = (params?: FeedsListQueryParams) =>
  [{ url: "/feeds" }, ...(params ? [params] : [])] as const;

export type FeedsListQueryKey = ReturnType<typeof feedsListQueryKey>;

export function feedsListQueryOptions(
  params?: FeedsListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = feedsListQueryKey(params);
  return queryOptions<
    FeedsListQueryResponse,
    ResponseErrorConfig<FeedsList500>,
    FeedsListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return feedsList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /feeds}
 */
export function createFeedsList<
  TData = FeedsListQueryResponse,
  TQueryData = FeedsListQueryResponse,
  TQueryKey extends QueryKey = FeedsListQueryKey,
>(
  params?: FeedsListQueryParams,
  options: {
    query?: Partial<
      UseBaseQueryOptions<
        FeedsListQueryResponse,
        ResponseErrorConfig<FeedsList500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? feedsListQueryKey(params);

  const query = useQuery(
    () => ({
      ...(feedsListQueryOptions(
        params,
        config,
      ) as unknown as UseBaseQueryOptions),
      queryKey,
      initialData: null,
      ...(resolvedOptions as unknown as Omit<UseBaseQueryOptions, "queryKey">),
    }),
    queryClient ? () => queryClient : undefined,
  ) as UseQueryResult<TData, ResponseErrorConfig<FeedsList500>> & {
    queryKey: TQueryKey;
  };

  return query;
}
