/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../kubb-client.ts";
import type {
  TagsListQueryResponse,
  TagsList500,
} from "../../types-generated.ts";
import type {
  QueryKey,
  QueryClient,
  UseBaseQueryOptions,
  UseQueryResult,
} from "@tanstack/solid-query";
import { tagsList } from "../client/tagsList.ts";
import { queryOptions, useQuery } from "@tanstack/solid-query";

export const tagsListQueryKey = () => [{ url: "/tags" }] as const;

export type TagsListQueryKey = ReturnType<typeof tagsListQueryKey>;

export function tagsListQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = tagsListQueryKey();
  return queryOptions<
    TagsListQueryResponse,
    ResponseErrorConfig<TagsList500>,
    TagsListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return tagsList({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /tags}
 */
export function createTagsList<
  TData = TagsListQueryResponse,
  TQueryData = TagsListQueryResponse,
  TQueryKey extends QueryKey = TagsListQueryKey,
>(
  options: {
    query?: Partial<
      UseBaseQueryOptions<
        TagsListQueryResponse,
        ResponseErrorConfig<TagsList500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? tagsListQueryKey();

  const query = useQuery(
    () => ({
      ...(tagsListQueryOptions(config) as unknown as UseBaseQueryOptions),
      queryKey,
      initialData: null,
      ...(resolvedOptions as unknown as Omit<UseBaseQueryOptions, "queryKey">),
    }),
    queryClient ? () => queryClient : undefined,
  ) as UseQueryResult<TData, ResponseErrorConfig<TagsList500>> & {
    queryKey: TQueryKey;
  };

  return query;
}
